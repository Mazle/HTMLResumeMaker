Руководство по запуску приложения в Windows
1. Установите jdk и Maven, осуществив настройки  в соответствии с рекомендациями разработчика.
2. Запустите командную строку из корневого каталога проекта.
3. В командной строке наберите:
mvn package
4. в папку проекта src/main/java/ поместите properties файл с необходимыми данными для резюме.
5. Поместите файл log4j.properties с необходимой конфигурацией в папку target (на один уровень с jar файлом). При отсутствии
данного файла будет загружена конфигурация по умолчанию.
6. При успешном завершении пункта 3 запустите командную строку из каталога target и наберите:
java -jar HTMLResumeMaker-jar-with-dependencies.jar ../src/main/java/{NAME_OF_PROPERTIES_FILE}
где {NAME_OF_PROPERTIES_FILE} - имя файла без расширения.
7.Откройте сформированный программой файл outfile.html

Руководство пользователя
Для нормальной работы программы требуется действующее подключение к интернету
Программа создает html версию резюме по жестко заданному шаблону, используя в качестве источника данных - properties
file.
Чтобы внести свои данные - создайте свой properties file или отредактируйте имеющийся (src/MyContent.properties).
Примечание: если в одной из граф (по одному ключу) планируется размещение нескольких значений, то используйте \n в
качестве разделителя.
Примечание: для добавления ассоциативного массива воспользуйтесь следующей формой:
ИМЯ_ПОЛЯ = KEY:VALUE,KEY:VALUE,KEY:VALUE, ... ,KEY:VALUE